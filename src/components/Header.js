import React from 'react';
import Title from './Title';

function Header() {
	return (
		<header className='header'>
			<Title className='header__title' text='Splitter' />
		</header>
	);
}

export default Header;
